// Conditional ada 2
// 1. If Else
// di If Else membandingkan menggunakan <, >, <=, >=, dan yang perlu diingat apabila = harus menggunakan == atau ===
// untuk menghubungkan dua conditional bila dan menggunakan &&, bila atau menggunakan ||
if (condition) {
    
} else if(condition){
    
} else{

}

// 2. Switch case
// ini digunakan apabila variabel yang dibandingkan hanya satu saja 
switch (key) {
    case value:
        
        break;
    case value:
        
        break;

    default:
        break;
}